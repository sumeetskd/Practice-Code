jQuery

Selection of element(using id):-

("#id_name").click()    -> to event mouse click triggered 
("#id_name").before()

$this => ivokes current html element

# -> used to access 'id'
. -> to access class
p -> to directly access paragraph elements